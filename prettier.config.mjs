/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
    printWidth: 200,
    singleQuote: true,
    trailingComma: 'all',
    tabWidth: 4,
};

export default config;
